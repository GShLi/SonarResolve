# 排除规则的覆盖配置文件
# 此文件的规则会覆盖 exclude_sonar_rule 文件中的配置
# 优先级：override > 基础配置
# 
# 使用场景：
# - 临时启用某些在基础配置中被排除的规则
# - 特殊项目需要不同的排除策略
# - 测试特定规则的修复效果

# 格式说明：
# ENABLE:rule_id   - 强制启用某个规则（即使在基础配置中被排除）
# DISABLE:rule_id  - 强制禁用某个规则
# rule_id          - 等同于 DISABLE:rule_id
